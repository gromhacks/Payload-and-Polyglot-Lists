services:
  el-injection-java:
    build: ./app
    ports:
      - "8041:8080"
    networks:
      - testbed-net
    restart: unless-stopped

networks:
  testbed-net:
    external: true
    name: testbed-net
