version: "3.8"

services:
  deserialization-php:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "8022:8080"
    networks:
      - testbed-net
    restart: unless-stopped

networks:
  testbed-net:
    external: true
    name: testbed-net
