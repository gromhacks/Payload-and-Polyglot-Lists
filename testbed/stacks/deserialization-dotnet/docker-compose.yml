services:
  deserialization-dotnet:
    build: ./app
    ports:
      - "8045:8080"
    networks:
      - testbed-net
    restart: unless-stopped

networks:
  testbed-net:
    external: true
    name: testbed-net
